generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id        String   @id @default(uuid())
  title     String
  sku       String   @unique
  slug      String
  price     Float
  url       String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  pictures        Picture       @relation(fields: [picturesId], references: [id])
  picturesId      String        @unique
  description     Description   @relation(fields: [descriptionId], references: [id])
  descriptionId   String        @unique
  productDetail   ProductDetail @relation(fields: [productDetailId], references: [id])
  productDetailId String        @unique
}

model Picture {
  id        String   @id @default(uuid())
  smallPic  String
  largePics String[]

  product Product?
}

model Description {
  id                String @id @default(uuid())
  description_short String
  description_long  String

  product Product?
}

model ProductDetail {
  id           String @id @default(uuid())
  category     String
  manufacturer String
  hotness      Float
  weight       Float

  product  Product?
  rating   Rating   @relation(fields: [ratingId], references: [id])
  ratingId String   @unique
}

model Rating {
  id            String @id @default(uuid())
  average       Float
  nr_of_reviews Int

  ProductDetails ProductDetail?
}
